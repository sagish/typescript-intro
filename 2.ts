`Primitives & Built-in Types`

let username: string = 'string';
let age: number = 16;
let isTired: boolean = true;
let FETCHING: symbol = Symbol('FETCHING');
let random: any = true // 2, null

enum Color { Red, Blue, Green }
let myColor: Color = Color.Red;

let titles: string[] = ['Lord of the Rings', 'Apocalypse Now', 'Twilight']
let tupple: [number, string, Color] = [1, 'Hello', Color.Red];
