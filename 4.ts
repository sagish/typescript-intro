`Built-in definitions`

const delayed = new Promise(resolve => setTimeout(resolve, 100))
  .then


const inputs = document.getElementsByTagName('input');
inputs[0].value

window.onblur
document.getElementById